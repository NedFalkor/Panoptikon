import { DisplayIfDirective } from './display-if.directive';

describe('DisplayIfDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayIfDirective();
    expect(directive).toBeTruthy();
  });
});
